{
  "dependencies": {
    "sergey": "^0.0.13"
  },
  "name": "maxeonyx-website",
  "version": "1.1.0",
  "description": "My personal website",
  "main": "src/index.html",
  "repository": "https://github.com/maxeonyx/website",
  "author": "Maxwell Clarke (Maxeonyx) <maxeonyx@gmail.com>",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "sergey --root=./src/ --imports=imports --content=content --exclude=content --output=../public/",
    "dev": "sergey --root=./src/ --imports=imports --content=content --exclude=content --output=../public/ --watch"
  }
}
